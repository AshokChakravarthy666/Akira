name: telegram-bot
service:
  name: telegram-bot
  env: python
  buildCommand: pip install -r requirements.txt
  startCommand: python main.py
  ports:
    - port: 8080
  scaling:
    min: 1
    max: 1